import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DialogBoxx = () => {
  return (
    <View>
      <Text>DialogBoxx</Text>
    </View>
  );
};

export default DialogBoxx;

const styles = StyleSheet.create({
  main: "red",
  flex: 1,
});
